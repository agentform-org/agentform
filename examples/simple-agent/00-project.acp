// Simple ACP Example
// An agent that can perform basic tasks using an LLM
//
// This example demonstrates multi-file ACP support.
// Files are processed in alphabetical order, so we use numbered prefixes.

acp {
  version = "0.1"
  project = "simple-agent-example"
}
